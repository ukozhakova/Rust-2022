// this is how to use the local dependency
// please note that we cannot use the "-" character
// but we need to change its occurrence to an underscore "_"
use database_layer::add;

/*
 *
 *         Please showcase the usage of the
 *         database_layer library here
 *
 *         Feel free to remove all the code 
 *         and start with the implementation
 *
 */

fn main() {
    println!("Hello, world! {}", add(10, 32));
}
